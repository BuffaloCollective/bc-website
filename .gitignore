.DS_Store
bc_build_spec.md
content.md

# Local backup of uncompressed source textures — not used by the site.
assets/images/textures-original/

# Local Claude Code preview tooling — not used by the site.
.claude/

# Retired pages kept on disk for easy restore — not part of the live site.
Archived Content/
